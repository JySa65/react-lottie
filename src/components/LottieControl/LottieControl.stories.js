import * as React from 'react'
import Lottie from ".";

export default {
  title: "Lottie/LottieControl",
  component: Lottie,
};

const Template = (args) => <Lottie {...args} />;

export const LottieControl = Template.bind({});
