import * as React from "react";
import Lottie from ".";

export default {
  title: "Lottie/TransitionWithOptions",
  component: Lottie,
};

const Template = (args) => <Lottie {...args} />;

export const TransitionWithOptions = Template.bind({});
