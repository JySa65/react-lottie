import * as React from 'react'
import Lottie from ".";

export default {
  title: "Lottie/ToggleLike",
  component: Lottie,
};

const Template = (args) => <Lottie {...args} />;

export const ToggleLike = Template.bind({});
