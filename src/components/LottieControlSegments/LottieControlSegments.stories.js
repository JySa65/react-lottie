import * as React from 'react'
import Lottie from ".";

export default {
  title: "Lottie/LottieControlSegments",
  component: Lottie,
};

const Template = (args) => <Lottie {...args} />;

export const LottieControlSegments = Template.bind({});
