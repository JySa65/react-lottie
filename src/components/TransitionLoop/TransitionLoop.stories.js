import * as React from "react";
import Lottie from ".";

export default {
  title: "Lottie/TransitionLoop",
  component: Lottie,
};

const Template = (args) => <Lottie {...args} />;

export const TransitionLoop = Template.bind({});
